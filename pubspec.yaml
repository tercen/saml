name: saml
description: Utility library for saml
version: 1.1.6

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  uuid:
  xml: ^6.6.0
  rsa_pkcs:
  pointycastle:

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
